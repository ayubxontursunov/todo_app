path('del/<str:items_id>',remove,name='del'),

bu narsa frontdan del dan items_id yoziladi

views ichidan remove(items_id) uchun 'del/<str:items_id>' items_id yozilgan bu narsa frontdan keladi.


item_list = Todo.objects.order_by('-date')

oxirgi qo'shilgan date lar birinchi bo'lib chiqishi uchun